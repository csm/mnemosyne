# Minimal runtime for the pre-built grader binary (see build.sh, run by
# run.sh before this Docker build). Build context is this directory only.
FROM debian:bookworm-slim
COPY bugfix-clj-2-grader /usr/local/bin/bugfix-clj-2-grader
